import { ReactNode } from "react";

export interface MainLayoutProps {
    children?: ReactNode;
}

export interface TopNavProps {
    children?: ReactNode;
}

export interface ApiConfig {
    imgConvertUrl: string;
}
